# SideNote

SideNoteは、[Obsidian](https://obsidian.md)のプラグインで、ノートにコメントを追加することができます。これらのコメントは専用のサイドペインに表示され、メインのテキストを乱雑にすることなく、注釈を簡単に確認・ナビゲートできます。コメントはエディタ内で直接ハイライト表示されるため、視覚的に素早く参照できます。

## 主な機能

### 基本機能

- **選択範囲へのコメント追加**: Markdownノート内の選択したテキストに簡単にコメントを追加できます
- **2つの表示モード**:
  - **サイドバーモード**: 右サイドバーにコメントを開いて常時表示
  - **スプリットビューモード**: ノートの横に分割ペインとしてコメントを開く
- **視覚的なハイライト**: コメントしたテキストがエディタ内で自動的にハイライト表示（黄色の背景と下線）
- **自動追跡**: ノートを編集してもハッシュベースのマッチングによりコメントが自動的にテキストを追跡
- **クリックで移動**:
  - サイドペインの任意のコメントをクリックすると、エディタ内の該当箇所にジャンプ
  - コメントを**ダブルクリック**すると直接編集モーダルが開く
  - エディタ内のハイライトされたテキストをクリックすると、サイドバーが開き対応するコメントがハイライトされます
- **内部リンク対応**: コメント本文内の `[[WikiLink]]` がクリック可能になり、リンク先のノートを開ける
- **検索フィルター**: サイドバー上部の検索欄にキーワードを入力するとコメントをリアルタイム絞り込み（ハイライトテキストとコメント本文が対象。Current File・All Notes 両モードで動作）
- **編集と削除**: サイドペインから直接コメントを管理できます
- **キーボードショートカット**: コメントモーダルで`Cmd/Ctrl + Enter`で保存して閉じる、`Esc`でキャンセル、モーダル外をクリックで閉じる
- **柔軟なソート**: コメントをファイル内の位置順または作成日時順で並べ替え可能
- **オーファン化されたコメントの管理**: 元のテキストが削除されると、コメントは「オーファン化」され、個別に管理できます

### 高度な機能

- **Operational Transformation 追跡**: コメントの位置は絶対文字オフセットとして保存され、CodeMirror 6 の `ChangeSet.mapPos()` を使ってあらゆるドキュメント編集に追従します。
- **正確な重複テキスト対応**: 同じ文字列が複数箇所に存在しても、各コメントは自分のテキストインスタンスを追跡します。ハイライトが別の出現箇所に移動することはありません。
- **即時オーファン検出**: コメント対象テキストが削除されると範囲が直ちに潰れ（collapse）、別の出現箇所に移ることなく元の位置に赤いマーカーが表示されます。
- **Undo での復帰**: Ctrl+Z で削除したテキストを復元すると、ファイル保存時にコメントが自動的に復帰します。
- **アクティブファイルの自動更新**: サイドバーモード使用時、ファイルを切り替えるとコメントビューが自動更新
- **オーファン化されたコメントのハイライト**: 削除されたテキストの位置に赤い文字1文字でマーク（設定でオフ可能）
- **オプションのMarkdown保存**: コメントをノートごとのサイドノートMarkdownファイルに保存可能（デフォルトは `side-note-comments` フォルダ）

## 使い方

### コメントの追加

1. エディタで**テキストを選択**（推奨は3文字以上、正確性のためには10文字以上）
2. 選択したテキストを**右クリック**し、「Add comment to selection」を選択
   - またはコマンドパレット（`Cmd/Ctrl + P`）→「Side Note: Add comment to selection」
3. 表示されるモーダルにコメントを入力
   - `Cmd/Ctrl + Enter`で保存して閉じる
   - `Esc`またはモーダル外をクリックでキャンセル
4. テキストが自動的に黄色の背景と下線でハイライトされます

### コメントの表示

**オプション1: サイドバーモード**
- リボンのメッセージスクエアアイコンをクリック
- またはコマンドパレットから「Side Note: Open in Sidebar」を実行
- ビューはサイドバーに表示され、ファイルを切り替えると自動的に更新されます

**オプション2: スプリットビューモード**
- コマンドパレットから「Side Note: Open in Split View」を実行
- 現在のファイルのコメントを表示する新しいペインが右側に開きます

### コメント間の移動

- サイドペインの任意のコメントをクリックすると、エディタ内の該当箇所にジャンプ
- コメントを**ダブルクリック**すると直接編集モーダルが開く
- エディタ内のハイライトされたテキストをクリックすると、サイドバーが開き（まだ開いていない場合）、対応するコメントがハイライトされます
- コメントはテキスト内で直接ハイライトされているため、視覚的に簡単に参照できます

### コメントの検索

Side Note パネル上部の検索欄にキーワードを入力すると、コメントをリアルタイムで絞り込めます。ハイライトされたテキストとコメント本文の両方が検索対象です。Current File・All Notes の両モードで動作します。日本語・中国語・韓国語などの IME 入力にも完全対応しており、変換中に絞り込みが割り込むことはありません。

### コメントの管理

- **編集**: 各コメントの `...` メニューをクリック、またはコメントを**ダブルクリック**
- **削除**: 各コメントの `...` メニュー → Delete
- **ソート**: 設定 → Comment sort order で並び順を変更（位置順またはタイムスタンプ順）

### 設定

設定 → Side Note からアクセス：

- **Comment Sort Order**: ファイル内の位置順またはタイムスタンプ順を選択
- **Show Highlights in Editor**: エディタ内の視覚的なハイライトのオン/オフ切り替え
- **Store comments as Markdown files**: コメントをノートごとのサイドノートMarkdownファイルに保存
- **Markdown comments folder**: サイドノートMarkdownファイルを保存するフォルダ（Vault相対）を指定
- **Orphaned Comments**: オーファン化されたコメントの数を表示し、一括削除可能

## モバイル対応

**SideNote v1.0.3以降**は、iOS（Obsidian モバイルアプリ）とAndroidデバイスの両方に完全対応しました！

### モバイル機能

- **レスポンシブデザイン**: コメントモーダルはモバイル画面サイズに自動的に適応
- **タッチフレンドリーなボタン**: すべてのボタンに適切なタッチターゲットサイズ（最小44px）があり、簡単にタップ可能
- **モバイルキーボード最適化**: テキスト入力はモバイルキーボード向けに最適化され、iOSの自動ズーム防止のため16pxフォントサイズを使用
- **改善されたフォーカス管理**: タッチデバイスでのシームレスなモーダル操作のための、より良いフォーカス処理
- **テキスト選択サポート**: モバイルエディタでのテキスト選択とコメント追加に完全対応

### モバイルでコメントを追加する方法

1. ノートを編集モードで開く
2. コメントを付けたい**テキストを長押し**して選択
   - テキストの開始位置で長押しし、終了位置までドラッグします
3. **エディタ上部のツールバー**に表示される**メッセージアイコン（💬）**をタップ
   - ツールバーにアイコンが表示されない場合は、ツールバーを左右にスワイプしてください
4. コメント入力モーダルが表示されるので、コメントを入力
5. **Add**ボタンをタップして保存

### モバイルでコメント一覧を開く方法

#### **サイドバーでコメント一覧を表示**

**iOS（Obsidian iOS版）:**
1. 画面左上のハンバーガーメニュー（≡）をタップ
2. 右にスワイプするか、「Open in Sidebar」コマンドを実行
3. サイドバーにコメント一覧が表示される

**Android（Obsidian Android版）:**
1. 画面上部のメニュー（≡）をタップ
2. 「Side Note: Open in Sidebar」コマンドを実行（検索して実行）
3. 右側のパネルにコメント一覧が表示される

#### **コマンドパレットから直接開く**

1. 画面上部のメニュー（≡ または ⋮）をタップ
2. コマンドパレットを開く
3. 「side note」で検索
4. 「Side Note: Open in Sidebar」をタップ
5. サイドバーが開き、現在のノートのコメント一覧が表示される

### コメント一覧からコメントをクリック

- サイドバーで各コメントをタップすると、エディタ内でそのコメント位置にジャンプ
- コメント横の**編集（鉛筆）**アイコンをタップするとコメントを編集
- コメント横の**削除（ゴミ箱）**アイコンをタップするとコメントを削除

### モバイルのトラブルシューティング

- **ツールバーにメッセージアイコンが表示されない**: ツールバーを左右にスワイプして探してください。Obsidianのモバイル設定でツールバーのカスタマイズが可能です。
- **「Add」ボタンが反応しない**: ボタンをしっかりタップしてください。コメント欄にテキストが入力されていることを確認してください（空のコメントは保存できません）。
- **テキスト選択が困難**: より長いテキスト（10文字以上）を選択してください。より確実なマッチングが期待できます。
- **サイドバーが表示されない**: 画面上部のメニュー（≡）からコマンドパレットを開き、「Side Note: Open in Sidebar」を実行してください。

## 重要な注意事項

### テキスト追跡の制限事項

**短いテキスト（10文字未満）**: 非常に短いテキストへのコメントは動作が不安定になったり、近くの同一テキストに飛ぶことがあります。最良の結果を得るには、コメント時に少なくとも10文字以上を選択してください。

**重複テキスト**: OT ベースの追跡により、編集セッション中は各コメントが自分のテキストインスタンスに追従し続けます。Obsidian 再起動後やファイルを外部で編集した場合は ±10 行の近接検索で位置を再アンカーしますが、同一テキストが元の位置から 10 行以内に存在すると稀に誤ったインスタンスにマッチすることがあります。

### 今後の機能拡張

**ハイライトのバリエーション**: カスタマイズ可能なハイライトの色とスタイルを追加し、異なるコメントタイプに対応する予定です。

**リッチな編集UI**: 新しいMarkdown保存オプションの上で、ショートカットやプレビューなど、よりリッチなMarkdown編集体験を追加する予定です。

## 技術的詳細

- コメントは選択テキスト・SHA256 ハッシュ・位置座標とともに `data.json` に保存されます
- **ハイライト追跡**は Operational Transformation（OT）で実現: 位置を絶対文字オフセットとして保持し、ドキュメントへの変更のたびに CodeMirror 6 の `ChangeSet.mapPos()` で変換します — 編集セッション中にテキスト検索は行いません
- コメント対象テキストが削除されると範囲が潰れ（`from === to`）、別の同一文字列にドリフトすることなく即座にオーファン化されます
- ファイル保存時、Obsidian 外部で編集されたファイルに対して SHA256 ハッシュ検証つき ±10 行近接検索で位置を再アンカーします。ドリフトの原因となっていたフォールバックのドキュメント全体検索は削除されました
- エディタ内のハイライトには CodeMirror 6 のデコレーション機能を使用

## バージョン履歴

### 1.0.6
- **コメント内の `[[WikiLink]]` 内部リンクが動作しない問題を修正**（issue [#11](https://github.com/mofukuru/SideNote/issues)）
  - コメントテキスト内のリンクが正しくリンク先ノートを開くように修正
  - カスタムサイドバービューでは Obsidian のワークスペースレベルのリンクハンドラが有効にならないため、明示的な `openLinkText` ハンドラを追加
  - リンクのクリックが「エディタの該当箇所へジャンプ」動作と干渉しなくなった
- **サイドバーに検索フィルターを追加**（issue [#20](https://github.com/mofukuru/SideNote/issues)）
  - Side Note パネル上部に検索入力欄を追加
  - ハイライトテキスト・コメント本文をリアルタイムで絞り込み
  - Current File・All Notes の両モードで動作
  - 日本語・中国語・韓国語などの IME 入力に対応し、変換中に絞り込みが割り込まない
- **ダブルクリックで編集**（issue [#19](https://github.com/mofukuru/SideNote/issues)）
  - サイドバーのコメントをダブルクリックすると直接編集モーダルが開く。シングルクリックはエディタ位置へのジャンプのまま
- **ハイライト追跡を再設計**（issue [#23](https://github.com/mofukuru/SideNote/issues)）
  - テキスト検索ベースの位置管理を CodeMirror 6 の `ChangeSet.mapPos()` を使った Operational Transformation（OT）に置き換え
  - ハイライトが同一文字列の別の出現箇所にドリフトすることなく編集に追従するようになった
  - コメント対象テキストを削除すると即座にオーファン化（赤いマーカー表示）され、別の箇所にハイライトが移らなくなった
  - ハイライトドリフトの根本原因だったドキュメント全体ハッシュ検索（`findTextByHashOptimized`）を削除。Obsidian 外で編集されたファイル向けに ±10 行近接検索を残した
  - Undo（Ctrl+Z）でファイル保存時にコメントが自動的に復帰する
- **セキュリティ強化**（issue [#21](https://github.com/mofukuru/SideNote/issues)）
  - `normalizeCommentsFolderPath()` を追加: 絶対パスおよび `..` を含むパスを拒否し、デフォルトフォルダへフォールバック
  - Markdown ブロック解析の多行正規表現（`[^]*?`）を `indexOf` ベースの決定論的な処理に置き換え
  - イベントリスナーリークを修正: バインドしたクリックハンドラをクラスプロパティに保存し、`destroy` 時に正しく削除されるよう修正

### 1.0.5
- **コメントが重複して作成される問題を修正**（issue [#16](https://github.com/mofukuru/SideNote/issues/16), [#18](https://github.com/mofukuru/SideNote/issues/18), [#10](https://github.com/mofukuru/SideNote/issues/10)）
  - クリック/タッチイベントの二重実行を防ぐため、モーダルのsubmit処理を強化
  - 連続送信をブロックするsubmit再入防止ガードとデバウンスを追加
  - コメントの識別子をtimestampから安定したUUID（`id`）に移行し、正確なターゲット指定を実現
  - マークダウンマーカーをid形式に変更し、レガシーtimestamp形式へのフォールバックを維持
- **オーファン化されたコメントが復帰しない問題を修正**（issue [#15](https://github.com/mofukuru/SideNote/issues/15)）
  - オーファン化されたコメントもファイル更新のたびに再検索され、テキストが見つかれば自動的に復帰するように改善
- **「View all comments」コマンドを追加**
  - コマンドパレットから全ノートのコメントをファイルごとにまとめて表示できる新コマンドを追加

### 1.0.4
- **モバイル環境での座標ずれ問題を修正**
  - 動的テキスト検索の実装により、編集中もハイライトが常に正確な位置に表示されるように改善
  - Android/iOSで行編集時にハイライト位置がずれる問題を解決
  - ハイライト描画時に毎回テキストを検索することで、キャッシュされた座標の古さによるずれを防止
- **コメントクリック時のorphaned問題を修正**
  - コメントをクリックした際に不要な座標更新が実行され、コメントがorphanedになる問題を解決
  - 動的検索により座標更新が不要になったため、クリックハンドラーから座標更新処理を削除
- **削除時の確認モーダルを追加**
  - コメント削除ボタンを押すと確認モーダルが表示されるようになり、誤削除を防止
  - 「Cancel」と「Delete」の2つのオプションで安全に削除操作を実行

### 1.0.3
- **iOS/Androidデバイスの完全なモバイル対応**を追加
- **モバイルエディタツールバーにコメント追加ボタン**を追加（メッセージアイコン）
- コメントモーダルの改善とより良いフォーカス管理を実装
- タッチフレンドリーなインターフェース向けのモバイルレスポンシブCSSを追加
- テキスト選択検証の強化により、より良いエラーメッセージを提供
- モバイルデバイス向けのキーボードハンドリング最適化
- ボタンのタッチターゲットサイズを拡大（44px）してモバイルでの操作性を向上
- モバイルでのモーダルのスクロールと表示問題を修正
- iOSの自動ズーム防止のためフォントサイズ最適化（16px）を追加

### 1.0.2
- ハイライトされたテキストをクリックしてサイドバーを開き、コメントに移動する機能を追加
- コメントモーダルにキーボードショートカットを追加：
  - `Cmd/Ctrl + Enter`で保存して閉じる
  - `Esc`でキャンセル
  - モーダル外をクリックで閉じる
- コメント追加後にハイライトがすぐに表示されないバグを修正
- ハイライトをクリックしたときのビジュアルフィードバックを追加（サイドバーでコメントがハイライト）
- コメントをオプションでMarkdownファイルに保存できるように追加（設定でフォルダ指定可能、ノートごとのサイドノートファイル）
- Markdown保存を有効化した際に既存コメントをMarkdownへ移行
- ノートのリネーム時にサイドノートファイルもリネームし参照を同期
- 複数エディタを開いているときのハイライト位置ずれを修正

### 1.0.1
- ハッシュベースのテキスト追跡を追加し、堅牢なコメントアンカリングを実現
- 3段階マッチング戦略を実装（ハッシュ+近接度 → ファイル全体ハッシュ → オーファン化）
- CodeMirror 6デコレーションによるエディタ内ハイライト表示を追加
- オーファン化されたコメントの検出と管理機能を追加
- 2つの表示モード（サイドバーとスプリットビュー）を追加
- アクティブリーフ変更追跡による自動更新を追加
- 制限事項の警告を含む包括的なREADMEドキュメントを作成

### 1.0.0
- 初回リリース
- 基本的なコメント機能
- コメントの追加、編集、削除
- コメント表示用のサイドペインビュー

## ライセンス

このプラグインは、[MITライセンス](LICENCE)の下で公開されています。
